notes
.kiro

inmail.*
*.tgz
*.bz2
*.zip
venv

# Data files — large, reproducible from scripts
*.csv
*.jsonl
*.parquet
# JSON data files (but not config/source JSON)
data/interim/*.json
data/interim/**/*.json
trec07p
shared/

# Cache contents — reproducible by re-running lookup scripts
cache/whois/*.json
cache/spamhaus/*.json
cache/phishtank/url_hits.json

*.eml
*.pyc
__pycache__